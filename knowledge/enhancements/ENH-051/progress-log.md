# ENH-051: Progress Log

**2026-07-18** — Filed via `/kmgraph:kmg-start-issue-tracking` (Mode 3, Track only). Discovered mid-implementation on `v0.6.20-storage-migration-completion` Task 4 while fixing `kmg-init.md`'s stale global-topic KG path. ADR-066 already named this delegation as the intended root-cause fix but it was never operationalized as a buildable spec — confirmed neither `kg_config_init` nor `kg_scaffold` currently supports computing a path from a location choice. No branch created (Track-only). No GitHub issue filed yet — deferred pending user confirmation.
