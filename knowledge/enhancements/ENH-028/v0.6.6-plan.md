# v0.6.6 — Mandatory STOP Gate for Existing-KG Branch (ENH-028)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> ⚠️ `commands/` is PROTECTED. Modifications to `commands/kmg-init.md` and `commands/kmg-init-personal-kg.md` are **explicitly authorized by the user** for this plan. Do not ask for re-confirmation per task.

**Goal:** Add a MANDATORY STOP gate to `commands/kmg-init.md` and `commands/kmg-init-personal-kg.md` so LLMs cannot proceed past the existing-KG detection point without presenting the numbered menu and invoking the upgrade-inspector. Closes the compliance gap discovered in v0.6.5 live testing.

**Architecture:** Both init commands (`kmg-init.md` for project KGs, `kmg-init-personal-kg.md` for personal KGs) have an existing-KG detection section written as flowing conditional prose. The LLM treats this prose as optional context and skips the menu when forward momentum is high. The fix inserts a visually prominent STOP block with explicit all-caps barrier text immediately after the detection condition in each file. The upgrade-inspector module (`commands/kmg-init-shared/kmg-upgrade-inspector.md`) is correct and unchanged. ENH-028 spec acceptance criteria are marked done. A lessons-learned entry captures the compliance gap pattern for future reference.

**Tech Stack:** Markdown slash commands only (no TypeScript changes). No `npm run build` required.

---

## Prior Art

- **ENH-028** (`knowledge/enhancements/ENH-028/ENH-028-specification.md`) — governs this fix; defines gate text wording and acceptance criteria.
- **ENH-022 v0.6.5** — wired `kg_upgrade` inspect into the upgrade-inspector. That work is correct; ENH-028 closes the remaining gap where the inspector is never reached.
- **ADR-053** — `kmg-init-personal-kg.md` must mirror `kmg-init.md`; parity is a governance constraint, not a suggestion.
- **v0.6.5 live testing** — compliance gap discovered: LLM skipped the existing-KG menu and went straight to FTS5/wiki pass, bypassing the upgrade-inspector entirely.

## Recent Architectural Activity

- v0.6.5: `feat(init)` — wired `kg_upgrade` inspect into upgrade wizard (ENH-022 scope addition); fixed stale module paths in both init commands
- v0.6.5: `fix(init)` — replaced `_mcp_checked` global flag with per-category LLM-tracked flags and prose guards (Opus review)
- v0.6.0: `kmg-` prefix normalization (ADR-053) — all init command references must use `kmg-` prefix

---

## Global Constraints

- `commands/` is PROTECTED — modifications to `kmg-init.md` and `kmg-init-personal-kg.md` authorized for this plan only
- All slash command wording: plain English to user; no raw bash shown
- Gate wording must match ENH-028 spec reference wording exactly (the spec is the source of truth for the STOP block text)
- `kmg-init-personal-kg.md` STOP-block prohibition lines must be **identical** to `kmg-init.md` (the three all-caps sentences); menu body may substitute personal-KG-specific wording (no "categories", no quoted name). Rationale: full byte-for-byte identity would require identical menu options, but the personal KG path has no "Create new separate KG" option — the menus are structurally equivalent, not verbatim.
- The upgrade-inspector module (`commands/kmg-init-shared/kmg-upgrade-inspector.md`) is **not touched** — it is correct
- Fresh-install path (no existing KG detected) must be completely unaffected by this change
- Never commit plan files (`knowledge/enhancements/ENH-028/v0.6.6-plan.md` is gitignored)
- Branch `v0.6.6-init-compliance-gate` must be created from `main` (after v0.6.5 is merged)

---

## File Map

| File | Action | Reason |
|---|---|---|
| `commands/kmg-init.md` | Update | Replace prose conditional (§Pre-Wizard detection section) with STOP gate block + mandatory upgrade-inspector invocation; reconcile line 858 orphaned "Option 2 (Create new)" reference |
| `commands/kmg-init-personal-kg.md` | Update | Same STOP gate pattern at Step 1 (existing personal KG detection); must stay in parity with `kmg-init.md` per ADR-053 |
| `knowledge/enhancements/ENH-028/ENH-028-specification.md` | Update | Mark all acceptance criteria checkboxes done; update Status to Resolved |
| `knowledge/lessons-learned/patterns/Lessons_Learned_Patterns_LLM_Compliance_Gate_For_Branching_Commands.md` | Create | Capture the compliance gap pattern: prose conditionals are insufficient gates for LLMs; use explicit STOP blocks |

## Docs Updates (Grouped)

No user-facing Docusaurus pages, README, COMMAND-GUIDE, CHEAT-SHEET, GETTING-STARTED, CONCEPTS, or GLOSSARY pages require changes. This plan modifies `commands/` markdown (slash command internals) and `knowledge/` artifact files (spec, lessons-learned) only.

---

## Required Plan Steps

- [ ] **Step 0: Branch from correct parent**

After v0.6.5 is merged to `main`, create and check out the feature branch:

```bash
git checkout main && git pull
git checkout -b v0.6.6-init-compliance-gate
git branch --show-current   # expect: v0.6.6-init-compliance-gate
```

Branch placement (decision of record): branched from `main` after v0.6.5 merge. This feature is independent of any other in-flight work.

- [ ] **Step 0.2: Copy plan to `knowledge/plans/` for working reference**

```bash
cp knowledge/enhancements/ENH-028/v0.6.6-plan.md knowledge/plans/v0.6.6-plan.md
```

(Plan files are gitignored; do not commit.)

---

## Task 1: Add STOP gate to kmg-init.md

**Files:**
- Modify: `commands/kmg-init.md`

**Context:** The `## Pre-Wizard: Existing KG Detection` section currently reads as a conditional prose paragraph. The compliance gap is that nothing forces a STOP before setup steps run — the LLM reads the conditional and continues to the new-install wizard flow.

The fix replaces the prose conditional lead-in with a hard STOP block that precedes the menu, then restructures the option dispatch to make the upgrade-inspector call explicit and mandatory for Options 1 and 2.

⚠️ **Semantic change:** The live menu (line 38) has option 2 as "Create a new, separate knowledge graph." The new menu replaces this with "Check for issues — run upgrade inspector." This means the "Create new separate KG" flow (line 858: "Option 2 (Create new): Proceed to the full wizard") is removed from the existing-KG path. Confirm this is intentional before proceeding; see Step 1.3b below.

- [ ] **Step 1.1: Read the existing-KG detection section**

Read `commands/kmg-init.md` from `## Pre-Wizard: Existing KG Detection` through line 60 to confirm the exact current text and closing-fence location before editing. Do NOT use hardcoded line numbers — use the actual content as the match target.

- [ ] **Step 1.2: Replace the prose conditional with a STOP gate block**

Replace the text from `## Pre-Wizard: Existing KG Detection` through the end of the fenced menu block (verify exact closing-fence line via Read in Step 1.1). The replacement must be:

```markdown
## Pre-Wizard: Existing KG Detection

Check if a knowledge graph already exists for this project: look up `~/.claude/kg-config.json` for any entry whose path matches (or is a parent/child of) the current working directory.

**If NO existing KG is found:** skip this section entirely and proceed to the wizard (Step 1 below).

**If an existing KG IS found:**

> ⚠️ **STOP — EXISTING KG DETECTED**
>
> A knowledge graph named "[name]" already exists at [path].
>
> **You MUST present the menu below before proceeding.**
> **Do NOT run any initialization, scaffolding, FTS5, or wiki steps yet.**
> **Wait for the user to select an option.**

Present this menu to the user:

​```
A knowledge graph named "[name]" already exists in the config and is set as active.
It's [type] at [path] with categories: [list].

What would you like to do?

1. See what's new   — run upgrade inspector (recommended)
2. Check for issues — run upgrade inspector
3. Re-initialize "[name]"  ⚠️  destructive; resets categories, git strategy, etc.
4. Cancel — the existing KG is already set up

Enter option (1/2/3/4):
​```

**Do not proceed past this point until the user has entered a selection.**
```

- [ ] **Step 1.3: Rewrite the option dispatch block**

Replace `### Option 1: See What's New` and its sub-section (verify boundary via the Read done in Step 1.1 — do not use hardcoded line 51) with an explicit dispatch that covers all four options and makes the upgrade-inspector call mandatory for Options 1 and 2:

```markdown
### Option 1 or 2: Run Upgrade Inspector (MANDATORY)

**→ You MUST execute this shared module. Read `commands/kmg-init-shared/kmg-upgrade-inspector.md` and follow it exactly before running any other step.**

Parameters:
- `{KG_PATH}` = resolved path for this KG (from `~/.claude/kg-config.json` entry for the matched KG)
- `{kg_name}` = name key of the matched KG in kg-config.json
- `{KG_TYPE}` = type field from the KG config entry ("project-local" or "personal")
- `{categories}` = categories array from the KG config entry

After the upgrade-inspector completes, continue to section 1d (Platform config check) and 1f (legacy migration) below.

### Option 3: Re-initialize

Proceed to the re-initialization flow (existing behavior — archive first, then wizard).

### Option 4: Cancel

Exit immediately. Do not create or modify any files. Confirm to the user: "No changes made."
```

- [ ] **Step 1.3b: Reconcile orphaned line 858 reference**

After replacing the dispatch block, search for "Option 2 (Create new)" or "Create new" in `commands/kmg-init.md`:

```bash
grep -n "Option 2\|Create new\|separate knowledge" commands/kmg-init.md
```

If line 858 (or equivalent) still references "Option 2 (Create new): Proceed to the full wizard," update it to match the new menu numbering (Option 3 = Re-initialize, Option 4 = Cancel) or remove the stale paragraph entirely. The "Create new separate KG" flow no longer exists on the existing-KG path.

- [ ] **Step 1.4: Verify**

```bash
grep -n "STOP\|MUST\|upgrade-inspector\|kmg-upgrade-inspector" commands/kmg-init.md | head -20
```

Expected: STOP block present, upgrade-inspector reference mandatory-phrased, no stale `init-shared/` references.

- [ ] **Step 1.5: Commit**

```bash
git add commands/kmg-init.md
git commit -m "fix(init): add mandatory STOP gate to existing-KG detection — prevent upgrade-inspector bypass (ENH-028)"
```

---

## Task 2: Add STOP gate to kmg-init-personal-kg.md (parity with kmg-init.md)

**Files:**
- Modify: `commands/kmg-init-personal-kg.md`

**Context:** `kmg-init-personal-kg.md` Step 1 goes directly to `Read commands/kmg-init-shared/kmg-upgrade-inspector.md and follow it exactly` with no detection check or menu presented first. The compliance gap is the same as `kmg-init.md`.

ADR-053 parity constraint: copy the three STOP-block prohibition lines verbatim from Task 1's output. Menu body uses personal-KG-specific wording (no categories list, no quoted KG name, no "Create new separate KG" option — personal KGs have no create-separate-KG path). After inserting the gate, confirm the existing parameters block (lines 39-49 or equivalent) is retained below the new option dispatch — do not remove the `{KG_PATH}`, `{kg_name}`, `{KG_TYPE}`, `{categories}` parameters or the "continue to Step 8/Step 9" instruction.

- [ ] **Step 2.1: Read Step 1 of kmg-init-personal-kg.md**

Read `commands/kmg-init-personal-kg.md` lines 34–55 to confirm the exact current text of Step 1 before editing.

- [ ] **Step 2.2: Add existing-personal-KG detection and STOP gate**

Before the `**→ Execute shared module:**` line, insert the detection check and STOP gate. The resulting Step 1 must read:

```markdown
### Step 1: Check for existing personal KG

Check if a personal knowledge graph already exists: look up `~/.claude/kg-config.json` for any entry with `type: "personal"` at the target path (`~/.kmgraph/` by default).

**If NO existing personal KG is found:** skip this step and proceed to Step 2 (directory scaffolding).

**If an existing personal KG IS found:**

> ⚠️ **STOP — EXISTING PERSONAL KG DETECTED**
>
> A personal knowledge graph already exists at [path].
>
> **You MUST present the menu below before proceeding.**
> **Do NOT run any initialization, scaffolding, FTS5, or wiki steps yet.**
> **Wait for the user to select an option.**

Present this menu to the user:

​```
A personal knowledge graph already exists at [path].

What would you like to do?

1. See what's new   — run upgrade inspector (recommended)
2. Check for issues — run upgrade inspector
3. Re-initialize    ⚠️  destructive; resets templates and structure
4. Cancel — the existing personal KG is already set up

Enter option (1/2/3/4):
​```

**Do not proceed past this point until the user has entered a selection.**

#### Options 1 or 2: Run Upgrade Inspector (MANDATORY)

**→ You MUST execute this shared module. Read `commands/kmg-init-shared/kmg-upgrade-inspector.md` and follow it exactly before running any other step.**

Parameters:
- `{KG_PATH}` = resolved personal KG path
- `{kg_name}` = "personal"
- `{KG_TYPE}` = "personal"
- `{categories}` = ["architecture", "debugging", "patterns", "process"]
- `{preserve_active}` = true

**Cross-project platform-split check (section d):** Section d auto-routes to `~/.claude/CLAUDE.md § Platform Preferences` for personal KGs via the `{KG_TYPE}` = "personal" passed above — no extra parameters needed.

**After upgrade-inspector completes, always continue to Step 8 (content migration) and Step 9 (evidence seeding).** These run independently of the template upgrade check — an up-to-date template install does not mean me.md/rules.md have been populated.

#### Option 3: Re-initialize

Proceed to the existing re-initialization flow (archive first, then wizard).

#### Option 4: Cancel

Exit immediately. Do not create or modify any files. Confirm to the user: "No changes made."
```

- [ ] **Step 2.3: Verify parity**

```bash
grep -n "STOP\|MUST\|upgrade-inspector\|kmg-upgrade-inspector" commands/kmg-init-personal-kg.md | head -20
grep -A5 "STOP —" commands/kmg-init.md
grep -A5 "STOP —" commands/kmg-init-personal-kg.md
```

Expected: three mandatory-prohibition lines identical in both files.

- [ ] **Step 2.4: Commit**

```bash
git add commands/kmg-init-personal-kg.md
git commit -m "fix(init): add mandatory STOP gate to kmg-init-personal-kg.md — parity with kmg-init.md (ENH-028, ADR-053)"
```

---

## Task 3: Mark ENH-028 acceptance criteria as done

**Files:**
- Modify: `knowledge/enhancements/ENH-028/ENH-028-specification.md`

- [ ] **Step 3.1: Update Status and mark acceptance criteria**

Change `**Status:** Proposed` to `**Status:** ✅ Resolved in v0.6.6`.

Mark all `- [ ]` acceptance criteria items as `- [x]`.

- [ ] **Step 3.2: Commit**

```bash
git add knowledge/enhancements/ENH-028/ENH-028-specification.md
git commit -m "docs(ENH-028): mark acceptance criteria done; status → Resolved (v0.6.6)"
```

---

## Task 4: Add lessons-learned entry for the compliance gap pattern

**Files:**
- Create: `knowledge/lessons-learned/patterns/Lessons_Learned_Patterns_LLM_Compliance_Gate_For_Branching_Commands.md`

- [ ] **Step 4.1: Write the lessons-learned file**

Create the file with frontmatter and the following content:

```markdown
---
title: LLM Compliance Gate for Branching Commands
category:
  uri: patterns
---
# LLM Compliance Gate for Branching Commands

## Problem

When a slash command contains a branching conditional written as prose
("if X is found, present menu Y; otherwise proceed to wizard"), LLMs under
forward momentum skip the conditional branch entirely and jump to the primary
flow. Observed in v0.6.5 live testing: `kmg-init` detected an existing KG,
skipped the existing-KG menu, and ran the FTS5 rebuild and wiki pass as if it
were a fresh install. The upgrade-inspector was never reached.

The problem is not logic — the conditional is correct. The problem is framing:
prose conditionals read as ambient context, not mandatory execution barriers.

## Solution

Replace the prose conditional with a **MANDATORY STOP gate** using all three of:

1. **Visual prominence** — warning callout (blockquote with ⚠️) and ALL-CAPS keyword (STOP)
2. **Explicit prohibition** — name exactly what must NOT happen ("Do NOT run any initialization, scaffolding, FTS5, or wiki steps yet")
3. **Explicit requirement** — name exactly what MUST happen ("You MUST present the menu below before proceeding") and state that forward execution is blocked ("Do not proceed past this point until the user has entered a selection")

Applied in v0.6.6 to `commands/kmg-init.md` and `commands/kmg-init-personal-kg.md`
(ENH-028) to gate the existing-KG detection branch.

## When to Apply

Use this pattern for any command section where:
- A detection check can branch to a non-trivial interactive flow
- Skipping the branch causes meaningful harm (data loss risk, missed upgrade, bypassed safety check)
- The conditional is currently written as prose ("if X... then Y")

## Related

- ENH-028 — the specific compliance gap that prompted this lesson
- v0.6.5 live testing — compliance gap discovered during init of an existing project KG
- ADR-053 — parity constraint between kmg-init.md and kmg-init-personal-kg.md
```

- [ ] **Step 4.2: Commit**

```bash
git add "knowledge/lessons-learned/patterns/Lessons_Learned_Patterns_LLM_Compliance_Gate_For_Branching_Commands.md"
git commit -m "docs(lessons): capture LLM compliance gate pattern — prose conditionals insufficient for branching commands (ENH-028)"
```

---

## Task 5: Smoke test

- [ ] **Step 5.1: Run init wizard on existing project KG**

Invoke `/kmgraph:kmg-init` on a directory with an existing registered KG. Confirm:
- STOP block and menu appear before any setup step runs
- FTS5 rebuild and wiki pass do NOT start until after menu selection
- Option 1 invokes the upgrade-inspector and runs to completion
- Option 4 exits with no files created or modified

- [ ] **Step 5.2: Verify fresh-install path unaffected**

Invoke `/kmgraph:kmg-init` on a directory with NO existing KG. Confirm STOP block is not shown; wizard proceeds normally.

- [ ] **Step 5.3: Run init personal KG on existing personal KG**

Invoke `/kmgraph:kmg-init-personal-kg` when `~/.kmgraph/` is registered. Confirm STOP block and menu appear; Options 1/2 invoke inspector; Option 4 exits cleanly.

---

## Task 6: Branch confirm, push, PR gate

- [ ] **Step 6.1: Confirm branch and commit log**

```bash
git branch --show-current   # expect: v0.6.6-init-compliance-gate
git log --oneline main..HEAD
```

Expected: 4 commits (Tasks 1, 2, 3, 4).

- [ ] **Step 6.2: Push and open PR**

Stop here. Do not push or create PR without user instruction (approval gate per rules.md).

---

## Acceptance Criteria (Definition of Done)

Satisfies **ENH-028 spec** when:
- `kmg-init.md` has a visually distinct STOP block (⚠️ callout + all-caps STOP) immediately after existing-KG detection
- `kmg-init-personal-kg.md` has the identical STOP block (parity; ADR-053)
- Both files have the three mandatory-prohibition lines
- Options 1 and 2 in both files route explicitly and mandatorily to `commands/kmg-init-shared/kmg-upgrade-inspector.md`
- Option 4 exits with no file changes
- Fresh-install path completely unaffected in both files
- All ENH-028 acceptance criteria checkboxes marked done

Satisfies **parity constraint** (ADR-053) when:
- The three prohibition lines in `kmg-init-personal-kg.md` are byte-for-byte identical to `kmg-init.md`
- Both files ship in the same PR; no partial fix

---

## Parallelism

All tasks are sequential.

| Task | Description | Mode | Tier | Est. |
|---|---|---|---|---|
| 0 | Create branch from main (post v0.6.5 merge) | inline | standard-tier | 2 min |
| 0.2 | Copy plan to knowledge/plans/ | inline | standard-tier | 1 min |
| 1 | Add STOP gate to kmg-init.md | inline | standard-tier | 10 min |
| 2 | Add STOP gate to kmg-init-personal-kg.md (parity) | inline | standard-tier | 10 min |
| 3 | Mark ENH-028 acceptance criteria done | inline | standard-tier | 5 min |
| 4 | Add lessons-learned entry | inline | standard-tier | 5 min |
| 5 | Smoke test | inline | standard-tier | 10 min |
| 6 | Branch confirm, push, PR (user approval gate) | inline | standard-tier | 2 min |
