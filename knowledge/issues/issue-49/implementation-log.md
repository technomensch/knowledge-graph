# Implementation Log — issue-49

| Date | Entry |
|---|---|
| 2026-08-17 | Issue tracked. Root cause and Spec (3-part: template fix, protocol fix, `kg_upgrade` backfill category) drafted and reviewed across multiple rounds as commit-group c6/c7 in `knowledge/plans/v0.7.2-orchestration-plan.md`. Not yet implemented. |
| 2026-08-19 | Spec item 3 (`plan-status-drift` `kg_upgrade` backfix category) implemented on `v0.7.2-c6-issue-49-upgrade-category` (commit `f50f4160`, merged via `fd3ce895`): repairs stale `~/.claude/plans/` mirrors of `knowledge/plans/*.md` plan files. Two tiers — Tier A (mirror drift) auto-repaired one-direction (canonical → mirror), gated behind the shared `confirmBackfix` consent mechanism; Tier B (branch-merge inference) is report-only, never auto-applied. Wires all seven `upgrade.ts` integration points (`APPLY_ORDER`, `ApplyCategory` union, inspect/apply switches, zod schema, both KG-path-existence guards — fixing a pre-existing bug in one that discarded already-computed findings on a missing KG path along the way). Added to the `kmg-upgrade-inspector` wizard's `confirmBackfix` consent list and documented in `docs/reference/command-guide.md`. 30 new tests. Opus review the same day found and fixed 4 real post-implementation bugs (warnings-vs-upgrades[] routing, branch-merge false positives, and 2 others); a follow-up Fable pass (`c13275c7`) corrected stale `confirmBackfix` doc comments that still named only `capture-corruption` as the gate's consumer, and added a shared-gate regression test. |
| 2026-08-19 | Spec items 1-2 (prospective template/protocol fix) implemented: `commands/kmg-start-issue-tracking.md` Step 4.1's Safety Header redesigned from a single frozen `🔴 STOPPED (Waiting for Manual Approval of Step 1)` string to a real three-state field (`🔴 AWAITING APPROVAL` / `🟡 IN PROGRESS` / `✅ COMPLETE`); `skills/kmg-execute-plan/SKILL.md` Steps 1 and 7 now actually write those transitions to the plan file (Step 1 on first user approval, Step 7 only if every success criterion verifies, HALT otherwise), with Steps 3/6 carved out so the protocol's own STATUS write isn't reverted by its own integrity audit (commit `df3792e2`). Opus review the same day found real logic bugs in the shipped protocol text (Step 7 wrote COMPLETE unconditionally even on a failed criterion; a fictional example string; ambiguity about whether the `**STATUS:**` label survives rewrite) — fixed (`d48f491e`). A second Opus pass found `core/default-templates/.../governance-rules.md` carries a byte-identical copy of the same frozen-STATUS protocol with no Platform Guard — and, via its installed copy at `~/.kmgraph/governance-rules.md`, is the version actually live in Claude Code sessions across all projects, unlike `SKILL.md`'s Gemini/Antigravity-only copy. Mirrored the same Step 1/3/6/7 fix there, phrased conditionally rather than hardcoding this project's exact emoji states, since the template ships across projects (commit `a553108d`). Not yet synced to the installed `~/.kmgraph/governance-rules.md` copy — outside this repo/branch's scope, needs separate handling via this project's own sync/upgrade tooling. |
| 2026-08-19 | All four Spec items closed within this branch's scope. Backfill for the five already-known-stale `v0.7.1.x` plans (Blast Radius item 3) was done manually during the prior planning session (2026-08-17), predating this branch. Status updated to `resolved`; `branch:` corrected from `none` to `v0.7.2-issues-46-51` (this field was never updated after the issue moved from tracking-only into an active commit-group). |
