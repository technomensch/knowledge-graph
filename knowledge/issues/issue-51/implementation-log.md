# Implementation Log — issue-51

| Date | Entry |
|---|---|
| 2026-08-17 | Issue tracked. Confirmed by reading `commands/kmg-init-shared/kmg-upgrade-inspector.md` and `mcp-server/src/tools/upgrade.ts` directly: the wizard's hardcoded 6-entry allow-list has drifted from the tool's real 9-category apply enum, leaving `config-location` and `capture-corruption` findings visible in the wizard's pending-items list but structurally unreachable through `/kmgraph:kmg-init`. Not yet implemented. |
| 2026-08-19 | Implemented (commit `59c0caea`, worktree `v0.7.2-c8-issue-51-wizard-allowlist`): Step 0's category-mapping instruction inverted from an allow-list to a deny-list scoped to the two categories that are structurally not apply targets (`version-update`, `resolution`) — closing a gap where `resolution` would previously have built an `apply:` call that failed Zod validation for otherwise-legitimate fixes. `platform-split` deliberately left off the deny-list (a valid apply-enum member, but `kg_upgrade inspect` routes it through `warnings[]`, never `upgrades[]`, so Step 0's loop never encounters it). Wired `capture-corruption`'s `confirmBackfix` consent-bypass flag into the apply-call instruction. Added a comment-only drift guard in `upgrade.ts` at the `resolution`/apply-enum coupling site. Manual trace verified: `capture-corruption` and `config-location` now reach `_mcp_apply[]`; `platform-split` still never reaches `upgrades[]`. |
| 2026-08-19 | Opus review of `59c0caea` found a real consent-bypass gap the new `confirmBackfix` wiring made consequential: the "choose individually" flow never removed a declined item's category from `_mcp_apply[]`, so declining `capture-corruption` still silently applied it. Fixed with an explicit prune-on-decline instruction (commit `75b0f475`). A second Opus review pass on that fix found it incomplete in five more ways — the paired `mcp_covered` flag being wrongly cleared on prune (which would have routed declined items into the bash fallback instead), an inverted drift-guard comment, an undefined description-to-category mapping for pruning, undefined mixed accept/decline behavior for `templates`, and — most severe — that the original fix commit's message claimed to close the consent-bypass gap but only did so for "choose individually"; "Apply all" had no per-item prompt at all and could silently apply a destructive backfix on the strength of a one-line finding summary. All five fixed (commit `6e4717b0`), with explicit sequencing defined between the per-item prompt loop and the batched apply call. |
| 2026-08-19 | Line-reference/count correction (commit `410d911b`) after the c8 plan's design corrections shifted two locations cited in this issue's own description (`issue-51-description.md`). Status updated to `resolved`; `branch:` corrected from `none` to `v0.7.2-issues-46-51`. **Note:** commit `59c0caea`'s message contains a `Closes #51` trailer. `github-issue:` on this doc is `pending` — no GitHub issue has actually been filed for this local `issue-51` — so that trailer does not correspond to a real, linked GitHub issue for this work; it appears to be an unintentional collision with the local numeric ID. Left as-is (not rewriting existing commit history); flagged here so it isn't mistaken for a genuine GitHub issue reference if a real issue #51 exists in this repo's GitHub tracker. |
