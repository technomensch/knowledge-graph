# Implementation Log — issue-48

| Date | Entry |
|---|---|
| 2026-08-17 | Folded into `v0.7.1.5-issues-46-47-48-49` as commit-group c3, per user direction during orchestration planning for issues 46/47/48 + dependabot + update-patch. Originally tracked on its own branch `v0.7.1.6-issue-48-adr-dual-implementation` (2 docs-only commits); that branch's content was copied here via `git show`, not merged, so the original branch and its commits still exist and were not deleted. User noted `v0.7.1.5-issues-46-47-48-49` will likely need a rename later since it now covers multiple issues beyond its original name — not done yet, no commits/push this session. |\n| 2026-08-17 | Added a Backfix Requirement section to `issue-48-description.md` and `solution-approach.md`, per explicit user direction during issue-46's implementation session that each of issue-46/47/48 must carry its own requirement to migrate existing users' already-affected data, not just fix the code path going forward. For this issue: a future `kg_upgrade` migration must normalize existing command-path-created ADRs to the agent-path schema (safe structural reshape for `author`/`email`/`git:` nesting), without fabricating a value for `implements` where none was ever recorded. Deferred to this issue's eventual implementation plan, not implemented now (still tracking + proposal only). |
| 2026-08-17 | Issue tracked. Discovered as a byproduct of implementing issue-46 (parallel, in-progress work on `v0.7.1.5-issues-46-47-48-49` in the main checkout, not touched by this branch). Verified by direct file inspection: `commands/kmg-create-adr.md` never calls `kg_capture` (grep confirms all 3 hits are prose, not a call) and is a fully standalone implementation of filename/frontmatter/body generation, parallel to `agents/create-adr-agent.md`'s `kg_capture`-based path. Confirmed actual field-set divergence between the two (author/email nesting, `commit_short` presence, `implements` hardcoded to `null` in the command path vs. settable in the agent path via wizard question 9) — upgrades this from theoretical duplication risk to an already-manifested inconsistency. Filed as `type: Refactor` (not Bug) since neither path is broken — both produce valid, usable ADRs, just with different schemas. Not yet implemented — this issue's branch tracks and proposes only (`solution-approach.md` recommends Option A: command dispatches to agent), per explicit user direction to file as a full standalone issue (branch + GitHub issue + `solution-approach.md`) rather than implement now. |
| 2026-08-19 | Implemented as commit-group c3 on `v0.7.2-c3-issue-48-adr-dual-implementation` (worktree off `v0.7.2-issues-46-51`). Option A (preferred, per plan Step 3): `commands/kmg-create-adr.md` no longer passes `context_provided` — it dispatches to `create-adr-agent` with only the resolved level-routing flag and `$resolved_model`, and the agent runs its own full 9-question wizard directly with the user. Removed Steps 3-7 (~280 lines: 8Q wizard, filename generation, frontmatter/file write, index update, commit) and the two stale preamble lines describing a dispatch that never existed; replaced with a single "Step 3: Dispatch to create-adr-agent" section. Kept Steps 0-2 (KG resolution, snapshot gate, ADR numbering, git metadata) unchanged, per the plan's explicit scoping — these duplicate work the agent also does internally, but that redundancy was called out and accepted in the plan, not a bug this issue fixes. Updated the Checklist and Example Invocation sections in the command file, and the stale "v0.2.2 will refactor it" note in `agents/create-adr-agent.md`'s header, to match. Plan's original Step 5 (a `kg_upgrade` backfix reshaping `author`/`email` under `git:`) was deleted before implementation — corpus-verified against all 70 existing ADRs that no code path has ever produced that nesting, so the backfix would have invented a third, wrong schema. Added a regression guard (`scripts/check-adr-command-dispatch.sh` + `.test.sh`, 7/7 passing) asserting the command file never re-embeds frontmatter/commit logic and always contains the dispatch call — satisfies both Step 4 (regression guard) and Step 6 (tests) from the plan, since no `commands/*.md` test harness existed before this. Fixed one stale internal cross-reference in `docs/reference/command-guide.md` ("Step 3.8" → "Related Lessons wizard question"); no other docs needed changes — they describe user-visible behavior, which is unchanged. Backfix requirement (see `solution-approach.md`) corpus-verified against all 70 existing ADRs before implementation and found to rest on a fabricated premise (0/70 actually diverge) — deleted from the plan rather than built; `solution-approach.md` and this issue's own `## Backfix Requirement` section updated to record that supersession. |
| 2026-08-19 | Fable review of the just-merged dispatch collapse (commit `ea489f4f`) found the collapse wasn't quite complete and the anti-drift guard had real detection gaps: `kmg-create-adr.md`'s git-unavailable branch still instructed assembling a 5-field frontmatter block directly — the exact divergent-schema behavior the fix was supposed to eliminate; `check-adr-command-dispatch.sh`'s `context_provided` check was a fixed string, so a differently-quoted or `=`-separated re-introduction would pass silently (broadened to a quoting/`:`-vs-`=`-tolerant pattern); all five divergence markers were verbatim literals from the pre-fix file, so the guard could only catch a wholesale revert, not incremental re-addition with new phrasing (added five pattern-class markers, each cross-checked against both the real pre-fix and real current file); and the guard's own test suite was circular, never exercising it against the real pre-fix blob or real current file (added both). A follow-up Fable validation pass (commit `55594464`) found 3 more real edge cases in the negation-stripping heuristic that decides whether a pattern-marker hit is legitimate boundary prose or a real regression, and fixed them (position-aware negation scope, correct sentence-boundary handling around "e.g."). All findings independently re-validated against the live code before fixing, per this branch's review protocol. Status updated to `resolved` — no open items remain against this issue's scope. |
