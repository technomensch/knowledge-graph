# issue-26: Acceptance Criteria

- [ ] `commands/kmg-start-issue-tracking.md` Step 6.1 no longer references `docs/issue-tracker.md`.
- [ ] Step 6.1 instead references the actual live mechanism (the two `README.md` indexes, or a newly-built real tracker if option 2 is chosen).
- [ ] `grep -rn "docs/issue-tracker.md" commands/ agents/ skills/` returns no hits after the fix.
