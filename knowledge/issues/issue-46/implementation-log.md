# Implementation Log — issue-46

| Date | Entry |
|---|---|
| 2026-08-16 | Issue tracked. Scoped via Opus deep-dive subagent (agentId: ae187d4f06180abd7). Not yet implemented. |
| 2026-08-16 | Expanded mid-session: paperwork-audit's session-summary-currency check surfaced Manifestation B (duplicated frontmatter block), live in this issue's own snapshot artifact. |
| 2026-08-16 | Second Opus review pass (agentId: ab2c94344860e5824) validated all findings against current repo state: fixed 5+ stale citations, live-confirmed the ADR-side of both Manifestation A and B (broken README link; ADR-046 has wrong `status: Proposed` vs. real `Accepted`), found a second missed content-embedding site (Step 6 full-summary template), found the session update-in-place path is currently non-functional (metadata plumbing gap), found a third manifestation (C — filename-prediction algorithm divergence for dotted branch names), found `commands/kmg-create-adr.md` as a fourth PROTECTED site needing the same fix, and renumbered test-cases.md (had duplicate item numbers from the mid-session expansion). Plan and all doc files rewritten to incorporate. Not yet implemented — scope has grown substantially, flagged in plan Step 0 for reconfirmation before Step 1. |
| 2026-08-16 | Registered in `knowledge/issues/README.md` was not done (no index entry exists for issue-46/47 — flagged in D5, no `knowledge/issue-tracker.md` file exists in this repo either, despite `commands/kmg-update-issue-plan.md:78` expecting one). Noted as a minor paperwork gap, not blocking. |
| 2026-08-17 | Started implementation via superpowers:executing-plans. Steps 1-7 done: title-prefix stripped from `session-summary-agent.md` (201/603) and `create-adr-agent.md` (286); embedded frontmatter blocks removed from both session-summary templates (S4 snapshot + Step 6 full-summary) and `create-adr-agent.md` Phase 5. |
| 2026-08-17 | Step 8 correction: direct inspection found `commands/kmg-create-adr.md` never calls `kg_capture` — it's a standalone implementation with no second layer to double-generate against, so it has neither Manifestation A nor B. Second Opus review's "fourth site" claim was wrong (unverified inference). No edit made to this file. All doc files (description, solution-approach, plan) corrected to remove the false claim. Separate finding — two un-synced ADR-creation implementations exist — filed as its own issue (issue-48, GitHub #228) via a background agent (isolated worktree), not folded into issue-46. |
| 2026-08-17 | Manual verification (Step 15) performed: items 15/16/17/18 confirmed via mechanical re-sweep (zero doubled filenames, zero broken README links, zero genuine double-frontmatter remaining beyond the 2 known false-positives) and the full test suite (428/428, including item 18's second-same-day-capture case). During this sweep found `knowledge/sessions/README.md` itself still linked to the 8 pre-rename filenames (missed in Step 13/14's original pass, which fixed `decisions/README.md`'s ADR-046 link but not the sessions index) — fixed, and used the discovery to extend `applyCaptureCorruption`'s migration with a Pass 3 (README link repair) plus ADR-filename-doubling support (`DOUBLED_ADR_FILENAME`), since the original migration would have reproduced the exact same gap for other users. 3 new tests added (86/86 capture-corruption tests passing, 428/428 full suite). Items 13/14 (live `/kmgraph:kmg-session-summary` and `/kmgraph:kmg-create-adr` invocation against the synced plugin cache) deferred to merge-review — not run this session; the unit/integration suite exercises the same code paths at the function level with equivalent inputs. |
| 2026-08-17 | Repaired this repo's own 16 corrupted artifacts by hand (Steps 13-14): 7 doubled-frontmatter merges, 8 doubled-date filename de-duplications (1 overlapping both), 1 broken README link. Precise-detection audit correctly identified 2 additional files as false positives (single frontmatter block followed by a legitimate body horizontal-rule divider, not the bug) and left them untouched. User flagged: fixing capture.ts alone only helps *future* captures — existing users' already-corrupted files stay broken forever without a migration; this branch does not ship without one. Added `kg_upgrade`'s `"capture-corruption"` category (`mcp-server/src/tools/upgrade.ts`), generalizing the by-hand repair: conservative union-merge for frontmatter (never auto-resolves a real field conflict), exact-duplicate-only filename stripping (never guesses on a near-duplicate/rollover mismatch). 7 new tests added, all passing; full mcp-server suite 425/425 passing. |
| 2026-08-17 | Steps 9-12 implemented: `capture.ts` contract comment, defensive frontmatter strip (both write sites), metadata/zod plumbing for session (`as_of_commit`/`last_updated`) and ADR (`status`/`number`/`implements`/`related`/`search_aliases`/`git.pr`/`git.issue`), fixed `status: Proposed` hardcode, `displayTitleFor()` for README index display text. Discovered the ADR frontmatter branch had never actually matched `ADR-template.md`'s canonical shape (`date`/`deciders` instead of `created`/`author`) — was always masked because callers duplicated a correct block on top; fixed to match the template now that this is the sole generator. Wired `existingFile` into both session-summary-agent.md capture payloads, removed inert `"version": "append"`. Found and fixed Manifestation C at **three** sites (S1's existence check, Step 1.5's existence check, and a dead `session_filename` variable in Step 6) — all three used exact `${today}-${branch_slug}.md` string construction instead of matching the server's actual `slugify()`'d filename or its own date-only `checkExistingFile()` matching; the Step 6 site was found only by grepping after the first two were fixed. 60/60 capture.test.ts tests pass (2 pre-existing tests updated for the intentional `date`→`created`/`deciders`→`author` schema fix, 20 new tests added); full suite 418/418 passing. |
| 2026-08-17 | Opus review of the still-uncommitted diff found 2 critical bugs before commit: (1) `detectDoubledFrontmatter`'s false-positive guard only worked when the mistaken "block2" never found a closing `---` at all — a file whose body opens with a `---` divider *and* has a later `---` further down (a second section break) gave the old code a false closing line, so `applyCaptureCorruption` merged/deleted everything in between as if it were frontmatter. Reproduced live against this repo's own `knowledge/sessions/2026-05/2026-05-25-v0.6.0-phase-1-planning-multi-platform-decisions.md` (would have deleted ~10 real body lines, including two section headings, with no backup and no warning). Fixed by requiring block2 to contain at least one real `key:` field before treating it as frontmatter; added a `.bak` backup before the in-place rewrite; added 3 regression tests modeling the exact shape. (2) `DOUBLED_ADR_FILENAME` had no backreference tying the two ADR numbers together, so a file legitimately referencing a *different* ADR in its slug (e.g. `ADR-050-adr-046-followup-fix.md`) would be wrongly de-doubled, dropping the reference — no such file exists yet but the shape is plausible given ADR-046 already exists in this repo. Fixed with a digit-capturing backreference; added 2 regression tests (same-number still de-doubles, different-number left alone). Also fixed 2 same-file Important findings from the same review: `stripLeadingFrontmatter` (`capture.ts`) had the identical false-positive class (no YAML verification, no exact-fence check — a `----` dash run was being matched as a valid closing fence) — fixed with the same real-field check plus an exact-line match; and the session `title:` frontmatter had regressed to a bare undated value instead of reusing `displayTitleFor()`'s dated form (degrading FTS5 title-ranked search) — fixed to call `displayTitleFor("session", metadata)`. 8 new tests added across `upgrade.test.ts`/`capture.test.ts`; full suite 436/436 passing. Scope explicitly excluded 3 lower-severity same-review findings that touch files shared with not-yet-started plans (`create-adr-agent.md`, shared with c3/issue-48) or are docs-only — left for those plans. |
| 2026-08-18 | Independent second-model review (Fable, via pr-review-toolkit:code-reviewer) of the fixes above found the Critical #1 guard (`block2.order.length > 0`) was still narrower than needed: a single stray colon-prefixed prose line (e.g. `Note: deferred X.` at column 0), surrounded by real non-YAML content (headings, blank lines), still satisfied "at least one field" and would be merged/stripped as if it were frontmatter — reproduced live by the reviewer against both `detectDoubledFrontmatter` (`upgrade.ts`) and `stripLeadingFrontmatter` (`capture.ts`, no `.bak` on that path). Fixed by tightening both to require *every* non-blank line in the candidate block to be a `key:` field or an indented continuation of one (`looksLikeYaml`), not just one field present anywhere. 2 new regression tests added (one per file) modeling the exact reproduced shape. Full suite 440/440 passing, build clean. All other findings from the same review (Critical #1's original fix, Critical #2's regex backreference, the `.bak` write, `stripLeadingFrontmatter`'s other edge cases, the session `title:` fix) were independently confirmed correct as implemented, no changes needed. |
| 2026-08-19 | Post-merge Fable review of `capture.ts` (commit `94ff2897`) found the forward-fix guard was still incomplete on shapes it exists to handle: `stripLeadingFrontmatter` removed only one block (a legacy file already carrying doubled frontmatter, re-read through the update-in-place path, got a second doubled block re-stacked on top instead of cleaned up — now strips to a fixed point); the key-shaped-lines guard still deleted a prose-only region between `---` rules when every line happened to start with a colon-prefixed word (now additionally requires recognizing at least one key from the real generated-frontmatter vocabulary); an empty frontmatter block (bare `---`/`---` pair) leaked through unstripped; and Manifestation A (doubled filename prefix) had zero server-side enforcement, unlike Manifestation B — `handleCapture` now rejects a title carrying its own type's prefix pattern with `VALIDATION_ERROR` before any write. 13 new tests. A separate full-branch Fable pass (`1a0cc974`) additionally found `applyCaptureCorruption`'s first pass clobbered an existing `.bak` unconditionally instead of using the shared `backupOnce()` helper its sibling categories use (risking loss of the true original across a two-run wizard sequence), and unguarded `statSync`/`readFileSync` in the capture-corruption scan could throw on one bad directory entry and crash the entire inspect/apply call — both fixed. Items 13/14 (live `/kmgraph:kmg-session-summary` and `/kmgraph:kmg-create-adr` invocation against the synced plugin cache) remain deferred to merge-review, unchanged from the 2026-08-17 entry — this is the one open item keeping `status: in-progress` rather than `resolved`. |
