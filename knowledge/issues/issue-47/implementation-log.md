# Implementation Log — issue-47

| Date | Entry |
|---|---|
| 2026-08-16 | Issue tracked. Scoped via Opus deep-dive subagent (agentId: ae187d4f06180abd7). Not yet implemented. |
| 2026-08-16 | Second Opus review pass (agentId: ab2c94344860e5824) validated findings: all 4 original call sites confirmed at current line numbers, no fifth site found (repo-wide grep, widened to include docs/ hooks/ scripts/ core/ mcp-server/src). Fixed stale citations (reference pattern is skills/kmg-paperwork-audit/SKILL.md:29-41 not 30-44; "omit if empty" is line 485 not 487). Found a second existing correct copy at scripts/pre-push-gate.sh:119-127. Resolved the previously-open "where to put a shared snippet" decision (inline at each site, no shared-lib mechanism exists in this repo). Added required undeterminable-default-branch fallback per site. Flagged commands/kmg-update-issue-plan.md as PROTECTED. Found 2 docs-site references needing updates plus 3 drive-by errors in ADR-036 itself (path typos, Status field inconsistency). Plan and all doc files rewritten to incorporate. Not yet implemented. |
| 2026-08-16 | No index entry exists in `knowledge/issues/README.md` for issue-46/47; no `knowledge/issue-tracker.md` file exists in this repo despite `commands/kmg-update-issue-plan.md:78` expecting one. Noted as a minor paperwork gap, not blocking. |
| 2026-08-18 | Implemented in worktree `.worktrees/v0.7.2-c2-issue-47-diff-on-main`, branched off `41f6ae56` (post-c5). Steps 4-8 done: `agents/session-summary-agent.md` (2 sites), `skills/kmg-docs-impact-scan/SKILL.md`, `commands/kmg-update-issue-plan.md` (PROTECTED, permission gate cleared explicitly; kept on local `main`/`master` per user decision over `origin/main`), ADR-036 amended plus its 3 drive-by errors fixed. `docs/superpowers/specs/2026-04-16-docs-impact-scan-design.md` (Step 8's second docs-site reference) turned out to be untracked, gitignored local-only content (`.gitignore:77` excludes all of `docs/superpowers/`) — not part of this repo's actual tracked docs, so skipped rather than edited. Step 10.5's `diff-blank-reconstruction` category implemented in `mcp-server/src/tools/upgrade.ts`, wired through c5's shared `confirmBackfixCategory()` gate (same `confirmBackfix` param, no new one added) — 4 new regression tests covering correctly-blank/reconstructable/unresolvable/no-branch-recorded cases, using real `git init` fixtures rather than mocks. Step 9 manual verification done by extracting and directly executing each fixed site's bash logic against 3 real git states (on-default-branch, feature-branch, undeterminable-branch) rather than assuming correctness — caught and fixed a real bug in the process: the session-summary-agent.md fix originally used `git diff --name-only HEAD`/`--cached HEAD`, which silently misses brand-new untracked files (confirmed live); switched to `git status --porcelain --untracked-files=all`, matching what the plan's Step 4 point 5 actually specified. Step 10 re-grep confirms zero remaining live hardcoded `main...HEAD`/`origin/main..HEAD` sites (all remaining grep matches are explanatory prose/comments). Full mcp-server suite: 450/450 passing, clean `tsc`, clean build. |
| 2026-08-19 | Post-merge Fable top-to-bottom review of `upgrade.ts` (commit `1a0cc974`) found `diff-blank-reconstruction` silently misclassified every blank session file as unresolvable on any repo whose default branch isn't `main`/`master` — `resolveDefaultBranchForGit` returned `null` and nothing short-circuited on it; fixed to return empty cleanly, matching the existing `isInsideGitRepo` guard's posture. Same review also fixed a related shared-infrastructure bug: a failed `git merge-base`/`git diff` was indistinguishable from a genuinely empty diff, so `apply` could write a false "confirmed empty via git history" note into files whose history it never read (`gitDiffNameOnly` now signals failure with `null` instead of `[]`). `confirmBackfix`'s doc comments/Zod description were also corrected (`c13275c7`) to name `diff-blank-reconstruction` as one of the shared-gate's consumers, alongside `capture-corruption` and `plan-status-drift`, since they'd gone stale after this category joined the same boolean. No open items remain against this issue's own scope; status updated to `resolved`. |
